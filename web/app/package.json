{
  "name": "games-arcade",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "validate:browser": "vite build && node scripts/browser-validate.mjs"
  },
  "dependencies": {
    "slither-engine": "file:../slither-engine/pkg",
    "web-engine": "file:../engine/pkg"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.70",
    "playwright": "^1.61.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
