/// <reference types="vite/client" />

/** Injected by vite.config.ts: R2 URLs for the heavyweight payloads, keyed by
 * logical path (asset-manifest.json). */
declare const __ARCADE_ASSETS__: Record<string, string>;
