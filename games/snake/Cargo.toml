[package]
name = "snake"
version = "0.1.0"
edition = "2024"
description = "snake as a game-core Game"

[lib]
name = "snake"
path = "src/lib.rs"

[dependencies]
arrayvec = "0.7"
game-core = { workspace = true }
web-time = "1"

[dev-dependencies]
solvers = { path = "../../solvers" }
